# Base Packages

Shared base-layer packages for .NET web applications.

These packages contain reusable contracts and generic implementations for:

- domain entities and metadata contracts
- application service contracts and base services
- data access contracts and EF Core base repositories
- DTO contracts and implementations

Package versions are intended to be kept in sync across all `Base.*` packages.
